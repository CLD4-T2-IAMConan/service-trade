apiVersion: v1
kind: Service
metadata:
  name: {{ include "serivce-trade.fullname" . }}
  labels:
    {{- include "serivce-trade.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "serivce-trade.selectorLabels" . | nindent 4 }}
