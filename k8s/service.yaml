apiVersion: v1
kind: Service
metadata:
  name: template-service
  labels:
    app: template-service
spec:
  type: ClusterIP
  selector:
    app: template-service
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
